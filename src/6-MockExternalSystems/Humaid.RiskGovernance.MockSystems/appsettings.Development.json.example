{
  "_comment": "Same Docker Postgres instance as the Workbench (port 5433) but this service only ever touches its own 'mock_systems' schema - see db/schema.sql. Copy to appsettings.Development.json.",
  "MOCK_SYSTEMS_POSTGRESQL_CONNECTIONSTRING": "Host=localhost;Username=postgres;Password=postgres;Database=risk_governance_db;Port=5433",
  "CORS_ALLOWED_ORIGINS": [ "http://localhost:5210" ],
  "APPLICATIONINSIGHTS_CONNECTION_STRING": "",
  "_comment2": "Optional locally - only set this to test the Key Vault config path itself (see Program.cs's UnderscoreKeyVaultSecretManager); not needed for normal local dev.",
  "PEP_KEY_VAULT": ""
}
